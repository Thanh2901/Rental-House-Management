services:
  postgres:
    image: postgres:15
    container_name: finance_db
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: root
      POSTGRES_DB: finance_service
    ports:
      - "7666:5432"
    volumes:
      - finance_data:/var/lib/postgresql/data

volumes:
  finance_data: